{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "s3sentinel.serviceAccountName" . }}
  labels:
    {{- include "s3sentinel.labels" . | nindent 4 }}
{{- end }}
